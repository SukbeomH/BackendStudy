import { UserResolver } from '../user.resolver';

describe('UserResolver', () => {
  let userResolver: UserResolver;

  beforeEach(async () => {});

  describe('createUser', () => {
    it('should return ""', () => {});
  });
});
